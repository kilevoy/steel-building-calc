# Следующие шаги

- ~~Добавить ещё 2-3 smoke-теста: truss возвращает результат, purlins возвращает кандидатов, beam-cell возвращает непустой результат.~~ Закрыто 2026-06-16: вместо слабых smoke-тестов закреплены acceptance/parity-тесты для `truss`, `purlins`, `beam-cell` и контрактные проверки сводки.
- ~~Проверить bundle size после следующего deploy.~~ Закрыто 2026-06-16: `npm run build` теперь проверяет стартовый bundle и отсутствие локальных путей.
- Продолжить улучшение карточек ввода для раздела единого здания.
- Не начинать новые крупные функции до укрепления тестовой базы.
- Отдельно проработать расчёт горизонтальной реакции `H`.
- Начать заполнять `knowledge/raw/source-register.md` реальными Excel и КМ-источниками.
- Извлечь из Excel-оракулов первые обезличенные тест-кейсы в JSON/MD.
- Заполнить draft-сценарии в `knowledge/wiki/parity/scenarios/` конкретными входами и ожидаемыми выходами.
- Начать с `SCN-COLUMN-001`, затем перейти к `SCN-TRUSS-001` и `SCN-PURLINS-001`.
- Извлечь из PDF-схем VELICAN `KM-004`...`KM-010` правила подсчёта рам, ферм, балок покрытия, крайних/средних колонн и фахверковых стоек.
- Визуально проверить `KM-004`, `KM-006`, `KM-009` и оформить первые сценарии подсчёта элементов здания.
- Проверить draft-сценарии `SCN-BUILDING-COUNT-001`...`003` по планам/разрезам и только после этого переносить правила подсчёта в TypeScript.
## Актуализация 2026-06-03

Пункт про добавление базовых smoke-тестов для `truss`, `purlins`, `beam-cell` уже не является первым приоритетом: по этим разделам есть acceptance/parity-тесты. Дальше важнее не просто добавлять “жив ли engine”, а расширять сверку с Excel по сценариям.

Ближайший рабочий план:

1. Оконные ригели: два сценария уже автоматизированы (`SCN-WINDOW-RIEGEL-001/002`). Следующий шаг — сопоставить top-10 выдачу `Лист1!B24:D33` / `B37:D46` со строками листа `Расчет`, чтобы безопасно извлечь коэффициенты гибкости, прочности и прогибов.
2. Подкрановая балка: три acceptance-сценария уже закреплены. Следующий шаг — решить вопрос `B19` vs `D19` для шага рёбер, затем проверить связь с колонной и количество подкрановых балок в Summary на КМ/Excel-сценариях.
3. Балка покрытия / балочная клетка: уточнить терминологию `ВБ`, `ГБ`, `К` и убедиться, что сводка не смешивает “балку покрытия” с балочной клеткой перекрытия.
4. Сводка: диагностическое количество колонн при кране уже защищено от двойного счёта торцевого фахверка. Следующий шаг — подтверждённый сценарий для переноса правила в массу/стоимость.
5. Единое здание начинать только после фиксации правил количества элементов и минимальной parity-матрицы по основным вкладкам.
