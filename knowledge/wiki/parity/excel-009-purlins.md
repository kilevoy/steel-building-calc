# EXCEL-009 — общий калькулятор прогонов

Источник: `EXCEL-009` из `knowledge/raw/source-register.md`.

Файл: `Общий калькулятор прогонов v2.0.xlsx`.

Статус ingest: описана структура workbook и основные наблюдаемые входы/выходы. Формулы в код не переносились.

## Назначение

Workbook используется как Excel-oracle candidate для модуля прогонов. Источник важен для сверки горячекатаных/трубных/швеллерных прогонов, ЛСТК МП350/МП390, шагов, масс, стоимости и ограничений по нагрузкам.

## Листы workbook

| Лист | Назначение по наблюдаемой структуре | Размер | Формулы |
|---|---|---:|---:|
| `Лист1` | основной ввод и вывод подобранных прогонов | `A1:AA160` | 224 |
| `ЛСТК МП350` | расчёт и подбор ЛСТК профилей МП350 | `A1:BYP55` | 86247 |
| `ЛСТК МП390` | расчёт и подбор ЛСТК профилей МП390 | `A1:BYK55` | 86246 |
| `Расчет` | основной тяжёлый расчёт прогонов | `A1:GHA442` | 1910438 |
| `Лист2` | справочные коэффициенты/таблицы | `A1:AT26` | 0 |
| `Ветер СП` | расчёт ветровых нагрузок по СП | `A1:AM74` | 231 |
| `Ветер по СП РК EN` | расчёт ветровых нагрузок по СП РК EN | `A1:AG48` | 88 |

## Наблюдаемые входы на `Лист1`

- город;
- уровень ответственности;
- тип кровли;
- пролёт;
- расчётный пролёт;
- длина;
- высота;
- уклон кровли;
- шаг рам;
- шаг стоек фахверка;
- тип местности;
- ветровая нагрузка;
- снеговая нагрузка;
- конструкция покрытия;
- профлист;
- снеговой мешок;
- перепад высот;
- размер существующего здания;
- протяжённость снегового мешка;
- экономика: сталь и цена;
- максимальный и минимальный шаг;
- установка тяжей;
- прогон под снегозадержание;
- прогон под ограждение;
- шаг распорок.

## Наблюдаемые выходы

На `Лист1` видны блоки:

- подобранные сечения;
- максимальный коэффициент использования;
- сечение;
- сталь;
- шаг;
- вес;
- стоимость;
- подобранные прогоны для стали МП350;
- подобранные прогоны для стали МП390;
- масса 1 п.м;
- общая масса прогонов на один шаг;
- общая масса прогонов на здание;
- масса черняка;
- масса оцинковки;
- масса с распорками.

## Наблюдаемые группы профилей

В workbook видны варианты:

- двутавр;
- труба;
- швеллер;
- ЛСТК `2ТПС`;
- ЛСТК `2ПС`;
- ЛСТК `Z`;
- стали МП350 и МП390.

## Важные примечания workbook

На `Лист1` видны текстовые примечания:

- коэффициенты использования "по умолчанию" согласованы с главным конструктором;
- несущая способность посчитана в MicroFE 2015;
- расчётная модель указана как шарнирно опёртая балка пролётом 6 м;
- коэффициент условия работы для `ПZ` и для `ПС/ТПС`;
- раскрепления для профилей приняты с шагом 0,4 м через волну профлиста или сэндвич-панели.

Эти утверждения зафиксированы как наблюдаемые подписи workbook. Их применимость к текущему коду требует отдельной инженерной проверки.

## Axial wind load

В текущем UI проекта есть предупреждение, что осевая ветровая нагрузка для прокатных прогонов пока задана временным допущением.

Структурный ingest `EXCEL-009` не является достаточным подтверждением методики axial wind load. Для снятия предупреждения нужен отдельный разбор формул и acceptance-сценарий.

## Проверка старых репозиториев по выбору 2ТПС / 2ПС / Z

Проверены локальные reference-репозитории:

- `GH-001 colonna`;
- `GH-004 VELICAN`.

Найдено, что выбор ЛСТК-прогонов в старой логике был богаче, чем текущая реализация `steel-building-calc`:

- ЛСТК рассматривается отдельными семействами `MP350/2ТПС`, `MP350/2ПС`, `MP350/Z`, `MP390/2ТПС`, `MP390/2ПС`, `MP390/Z`;
- для каждой группы выбирается семейный winner, а не только общий лёгкий top-10;
- в VELICAN/insi-контексте присутствуют oracle-only параметры: `deck/profile sheet`, `tie installation`, `brace step`, `LSTK prices`, `requiresPanelFilter`, раздельные массы `black/galvanized/braced`;
- для обычного сценария `SCN-PURLINS-001` workbook/oracle отдаёт `Z 350х2,5`, а `2ТПС` и `2ПС` дают `#N/A`;
- `GH-001 colonna`, ���� `docs/purlin-lstk-diagnostics.md` фиксирует важное различие: для workbook-parity ЛСТК использует профильный `default_coef`, а не пользовательский `maxUtilization` как общий множитель несущей способности.

В текущем `steel-building-calc` добавлен только безопасный diagnostic helper `src/calc/purlin/diagnostics.ts`. Он читает уже рассчитанный `PurlinOutput` и объясняет, какие семейства имеют кандидата, а какие нет. Helper не меняет подбор, нагрузки, формулы, коэффициенты и numerical output.

Требует отдельного решения перед переносом в расчёт:

- добавить ли в UI/engine параметры `deckProfile`, `tieInstallation`, `braceStep`;
- как именно применять `requiresPanelFilter`;
- должен ли `maxUtilization` влиять на ЛСТК или только на сортовой металл;
- как показывать пользователю причины отказа `2ТПС` и `2ПС` без перегрузки интерфейса.

## Связь с текущим модулем

Связанная страница модуля: `knowledge/wiki/modules/purlins.md`.

При будущей сверке нужно отдельно сравнивать горячекатаные/прокатные варианты и ЛСТК МП350/МП390, потому что workbook содержит разные большие расчётные блоки.

## Что требует проверки

- Сопоставить входы `Лист1` с текущим UI модуля прогонов.
- Проверить методику axial wind load для прокатных прогонов.
- Проверить сценарии со снеговым мешком.
- Проверить установку тяжей и шаг распорок.
- Проверить различие МП350 и МП390.
- Определить минимальный набор acceptance scenarios, чтобы не сравнивать весь workbook.
- Проверить внешние ссылки на `[1]СП 20` и `[1]СП РК EN`, если они участвуют в формулах.

## Правила дальнейшего использования

Не изменять Excel-файл. Из-за большого количества формул читать workbook выборочно и фиксировать только необходимые обезличенные сценарии.

Не переносить коэффициенты и формулы в код без отдельного задания, источника и проверки.
