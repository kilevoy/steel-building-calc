# Матрица Excel-parity по расчетным модулям

Дата среза: 2026-07-28.

Последний автоматический parity-аудит: 2026-07-28. Наборы для подкрановой балки,
оконных ригелей, ЛСТК/прокатных прогонов и балочной клетки прошли: 5 файлов,
33 теста. Ожидаемые значения берутся из generated workbook snapshots и frozen
fixtures; сами generated-файлы в рамках аудита не изменялись.

Цель страницы: перед переходом к разделу "Единое здание" показать, какие модули уже имеют автоматическую сверку с Excel-oracle, какие поля реально проверяются тестами, и где еще нельзя утверждать полное совпадение 1 в 1.

Важно: эта матрица не меняет расчетную логику. Она фиксирует уровень доверия к текущим acceptance tests.

## Сводная таблица

| Модуль | Excel / источник | Автоматический тест | Что уже проверяется | Статус |
|---|---|---|---|---|
| Колонна | `EXCEL-010` + frozen fixtures, `SCN-COLUMN-002` | `src/calc/engine.acceptance.test.ts` | 10 сценариев: `N_kN`, `M_kNm`, снег, ветер H/V, непустой список профилей; первый профиль, utilization и масса default-сценария | частичная parity-сверка |
| Ферма | `EXCEL-008`, `SCN-TRUSS-001` | `src/calc/truss/engine.acceptance.test.ts` | нагрузки, усилия по группам элементов, выбранные профили, массы, итоговая масса | хорошая parity-сверка одного сценария |
| Прогоны ЛСТК | `EXCEL-009`, `SCN-PURLINS-001`, `SCN-PURLINS-002`, `SCN-PURLINS-003` | `src/calc/purlin/engine.acceptance.test.ts`, `autoStep.test.ts` | расчетные нагрузки, `autoMaxStep_mm`, выбор `Z`-профилей, отказы `2ТПС/2ПС`, массы, раскладка по скату, добавочный прогон и ручное ограничение шага | хорошая parity-сверка трех сценариев |
| Прогоны прокатные | `EXCEL-009`, `SCN-PURLINS-001` | `src/calc/purlin/rolled.acceptance.test.ts` | осевая ветровая нагрузка как в Excel, победитель прокатного подбора, шаг, масса | частичная parity-сверка |
| Балка покрытия / балочная клетка | `EXCEL-007`, `SCN-BEAM-CELL-001`, `SCN-BEAM-CELL-002` | `src/calc/beamCell/engine.acceptance.test.ts` | сценарий 001: ГБ и `NO_SOLUTION` для ВБ/К; сценарий 002: ВБ, ГБ, К, принятые элементы и итоги | хорошая parity-сверка двух сценариев |
| Оконные ригели | `EXCEL-005`, `SCN-WINDOW-RIEGEL-001`, `SCN-WINDOW-RIEGEL-002` | `src/calc/windowRiegel/engine.acceptance.test.ts` | вертикальная/горизонтальная нагрузка, расчетные длины, наличие 10 вариантов, первые профили/стали/массы для двух типов окна | частичная parity-сверка |
| Подкрановая балка | `EXCEL-006`, `SCN-CRANE-BEAM-001`, `SCN-CRANE-BEAM-002`, `SCN-CRANE-BEAM-003` | `src/calc/craneBeam/engine.acceptance.test.ts` | профиль, utilization, масса, прогиб, геометрические/прочностные/устойчивостные блоки, крановые lookup-значения для 5 т, 10 т и двух кранов | хорошая parity-сверка трех сценариев |
| Сводка | КМ + все Excel-oracle | пока нет отдельного parity-теста | агрегирование результатов модулей требует отдельной модели входов | не готово для строгой parity |
| Единое здание | `KM-002...KM-010`, будущие сценарии | `src/building/layout.test.ts`, `src/building/unifiedLayout.test.ts`, `src/building/unifiedBuildingInput.test.ts` | черновая модель количества колонн/ферм/прогонов, КМ-подобной раскладки и мост `hasCrane -> main/endFachwerk` | требует инженерного подтверждения |

## Что уже можно считать закрепленным

1. У каждого основного расчетного раздела, кроме сводки, есть хотя бы один автоматический acceptance или smoke/parity тест.
2. Тяжелые Excel-generated модули подкрановой балки и оконных ригелей проверяются через публичный расчетный API, а не через UI.
3. Последний слой тестов для `beamCell`, `windowRiegel` и `craneBeam` опирается на generated expected values, а не на вручную продублированные числа.
4. Для прогонов закреплены важные Excel-поведения: `autoMaxStep_mm = 1500`, отказы семейств `2ТПС/2ПС` в сценарии `SCN-PURLINS-001`, выбор `Z 350x2,5`, масса с распорками и расчет осевой ветровой нагрузки для прокатного подбора.
5. Для фермы закреплено, что сценарий должен идти с `loadAddition_pct = 15`; прежнее расхождение профилей было ошибкой входных данных теста, а не формул.

## Где еще нельзя говорить "полностью 1 в 1"

### Колонна

Есть хорошая численная база по `N/M/снег/ветер`, а `SCN-COLUMN-002` закрепляет первый выбранный профиль default-сценария, utilization и Excel-массу с коэффициентом 1.15. Отдельно остается вопрос длин колонн для разных типов кровли и положения колонны.

### Ферма

Сценарий `SCN-TRUSS-001` хорошо закрепляет профильный подбор и массу, но горизонтальная реакция `H` не подтверждена Excel-oracle. Нельзя подставлять `H = 0` как окончательное инженерное решение.

### Прогоны

Сценарии `SCN-PURLINS-001`, `SCN-PURLINS-002` и `SCN-PURLINS-003` покрывают базовую ветку, добавочный прогон под снегозадержание и ручное ограничение максимального шага. Нужны дополнительные сценарии:

- снеговой мешок;
- прогон под ограждение;
- разрезной/неразрезной прогон, если это поведение есть в Excel и должно попасть в приложение;
- другие типы кровли и покрытия.

### Балка покрытия / балочная клетка

Сценарий `SCN-BEAM-CELL-002` закрывает базовую полную ветку: Excel дает допустимые ВБ, ГБ и К. Остаются нужны дополнительные варианты по другим типам перекрытий, шагам и конструктивной схеме.

### Оконные ригели

Проверяются первые наблюдаемые профили и нагрузки для двух сценариев, включая окно типа 2 с высотой 2 м. Нужно явно сопоставить top-10 выдачу `Лист1` со строками листа `Расчет`, прежде чем извлекать дополнительные диагностические коэффициенты: гибкость, прочность, прогибы. Также нужны сценарии для других конструкций окон.

### Подкрановая балка

Базовые сценарии 5 т, 10 т и два крана 10 т закреплены. Нужны сценарии:

- UI теперь явно различает входной шаг `B19` и подобранный шаг `D19`; инженерно подтвердить, какое значение использовать как итоговое для ведомости;
- связь с крановыми полями в модуле колонны.

UI уже содержит основные workbook-входы по крану, балке и коэффициентам. Отдельный риск остаётся не в доступности полей, а в трактовке результата шага рёбер и связке с колонной/сводкой.

## Рекомендуемый порядок следующих тестов

1. Разобрать длины колонн для разных типов кровли и положения колонны.
2. Добавить сценарий со снеговым мешком или прогоном под ограждение.
3. Проверить связь крановых полей подкрановой балки с модулем колонны.
4. Для оконных ригелей извлечь диагностические коэффициенты прочности/гибкости/прогиба, если нужна более строгая сверка.

## Правило для будущих изменений

Если модуль меняется по формуле, ранжированию или входным данным, сначала нужно понять, какой источник является oracle:

- generated expected из workbook;
- frozen fixture;
- вручную описанный сценарий в `knowledge/wiki/parity/scenarios/`;
- решение пользователя или ГИПа.

Если source of truth неясен, изменение нельзя маскировать обновлением expected values. Нужно занести вопрос в `knowledge/lint.md` или `knowledge/wiki/parity/known-differences.md`.
