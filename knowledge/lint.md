# Lint инженерной памяти

## Противоречия

- ~~Подсчёт основных колонн в текущем коде и PDF-draft-сценариях различается по торцевым осям. Код считает `edge/middle` по `interiorFrameCount`, а PDF-модель временно описывает все оси `N`. Подробности: `knowledge/wiki/parity/known-differences.md`.~~ Закрыто 2026-06-12: модель подтверждена пользователем («оставить как есть») поверх решения ГИПа от 2026-05-15. Без крана торцы — отдельная группа фахверка; с краном торцевые колонны — основные (реализовано в `unifiedLayout.ts`, ветка `hasCrane`, без двойного счёта). PDF-draft-модель «все оси N» остаётся справочной и в код не переносится. См. `knowledge/wiki/decisions/2026-06-12-end-frame-columns-confirmed.md`.
- ~~Расхождение `ВП`/`НП` в `SCN-TRUSS-001`: текущий engine выбирает более лёгкие профили `тр.200х160х5` и `тр.140х120х4` вместо Excel-значений `тр.200х160х6` и `тр.120х5`.~~ Закрыто 2026-05-14: расхождение было следствием неверного `loadAddition_pct` в acceptance-тесте (передавался 0, корректное значение из workbook — 15 %). Engine формулы не менялись, ферма теперь идёт 1-в-1 с Excel.

## Устаревшие сведения

Проверять после каждого крупного refactor, особенно страницы модулей и checkpoints.

## Страницы без источников

Большинство стартовых страниц описывают текущее понимание проекта и требуют привязки к источникам по мере добавления Excel/КМ/нормативных материалов.

## Открытые инженерные вопросы

- Проверить методику расчёта горизонтальной реакции `H`.
- Проверить временный axial wind load warning в purlins.
- ~~Убедиться, что `source-register.md` заполняется по мере добавления исходников.~~ Проверено 2026-06-12: реестр ведётся, все EXCEL-005…010, KM-002…010, GH-001…004 имеют ID, статус и место хранения.
- ~~Проверить, что raw-исходники не попадают в Git.~~ Проверено 2026-06-12: `git ls-files` не содержит xlsx/pdf/dwg/dxf/zip; в `knowledge/raw/` только README/.gitkeep, каталог защищён `knowledge/raw/.gitignore`.
- ~~Для `EXCEL-005` проверить внешние ссылки workbook на `[1]СП 20` и `[1]СП РК EN`.~~ Проверено 2026-06-12: в `src/calc/windowRiegel/workbook.generated.ts` внешних ссылок вида `[N]…!` нет — листы «Ветер СП» и «Ветер по СП РК EN» включены в книгу локально, ссылки разрешены на этапе генерации.
- Для `EXCEL-005` отдельно разобрать лист `типы окон` и связь с коэффициентами приведения момента. Примечание 2026-06-12: лист `windowTypes` (значения 1–5) присутствует в generated-книге и участвует в расчёте через вход `windowType` (ячейка `B8`); открытой остаётся только инженерная расшифровка связи типа окна с коэффициентами приведения момента.
- Для `EXCEL-010` сопоставить `Сводка` с текущим `DEFAULT_COLUMN_INPUT`.
- Для `EXCEL-010` проверить типы колонн `крайняя`, `средняя`, `фахверковая` и влияние уклона кровли на длины колонн.
- Для `EXCEL-010` проверить формирование `N/M` и крановые нагрузки.
- Для `EXCEL-008` проверить, есть ли в workbook расчёт горизонтальной реакции `H`.
- Для `EXCEL-008` расшифровать `ОРб`, `ОР`, `РР` и связать их с текущими терминами UI.
- Для `EXCEL-009` проверить методику axial wind load для прокатных прогонов.
- Для `EXCEL-009` выбрать минимальные acceptance scenarios для прокатных прогонов и ЛСТК МП350/МП390.
- Для `EXCEL-007` сопоставить текущий модуль Beam Cell с workbook-терминами `ВБ`, `ГБ`, `К`.
- Для `EXCEL-007` выбрать acceptance scenario по ВБ/ГБ/массе/стоимости.
- Для `EXCEL-006` сопоставить UI подкрановой балки с входами `Сводка`.
- ~~Для `EXCEL-006` выбрать acceptance scenario для одного и двух кранов.~~ Закрыто 2026-06-12: `SCN-CRANE-BEAM-001` (один кран, default) и `SCN-CRANE-BEAM-003` (два крана 10 т) приняты и автоматизированы в `src/calc/craneBeam/engine.acceptance.test.ts`.
- ~~Для `EXCEL-006` проверять, что generated engine остаётся lazy-loaded и не возвращается в стартовый bundle.~~ Подтверждено 2026-06-12 runtime-замером на production-сборке: без включённого крана workbook-чанк (2.7 МБ) не загружается; стартовая загрузка — 240 КБ JS. Проверять после крупных изменений сборки.
- Для `KM-002` и `KM-003` визуально проверить страницы, где текст PDF извлекается как `(cid:...)`.
- Для `KM-002` и `KM-003` извлечь обезличенные геометрические сценарии для будущего раздела единого здания.
- Для `SCN-COLUMN-001` подтвердить связь первого результата Excel `35 Б1 / С355Б` с выбором текущего engine.
- Для `SCN-COLUMN-001` проверить, как обрабатывать крановые поля с `#N/A`.
- Для `SCN-TRUSS-001` проверить интерпретацию `Шаг прогонов = 0` и `B13 = #DIV/0!`.
- Для `SCN-TRUSS-001` проверить, должна ли добавка `2*2*4.81` из Excel входить в acceptance-сравнение массы.
- Для `SCN-PURLINS-001` проверить временное допущение axial wind load для прокатных прогонов.
- Для `SCN-PURLINS-001` решить, сравнивать ли стоимость или только инженерные параметры.
- Для `SCN-BEAM-CELL-001` проверить, почему Excel возвращает `#N/A` для ВБ, К и итоговой массы.
- Для `SCN-BEAM-CELL-001` решить, подходит ли сценарий только для сверки ГБ.
- Для `SCN-WINDOW-RIEGEL-001` проверить связь типа окна `1` с текущим UI/engine.
- Для `SCN-WINDOW-RIEGEL-001` найти диагностические коэффициенты по гибкости, прочности и прогибам для выбранных профилей.
- Для `SCN-CRANE-BEAM-001` проверить расхождение между максимальным коэффициентом 85 % и коэффициентом подобранного сечения 54.64725962825525 %.
- Для `SCN-CRANE-BEAM-001` подтвердить, какой шаг рёбер использовать: исходный `B19` или подобранный `D19`.
- ~~Для `SCN-BUILDING-COUNT-001`...`003` решить модель торцов: отдельный `endFrame/fachwerk` или включение торцевых основных колонн в `edge/middle`.~~ Закрыто 2026-06-12: решение принято, см. раздел «Противоречия» и `knowledge/wiki/decisions/2026-06-12-end-frame-columns-confirmed.md`.

## Что проверить перед следующим крупным refactor

Сверить документацию с фактическим кодом, прогнать typecheck/lint/test/build, проверить стартовый bundle и GitHub Pages.
# Актуальное pending-решение

- Вопрос по классификации торцевых колонн закрыт: ответ ГИПа (`2026-05-15-gip-column-count-answer.md`) реализован в `unifiedLayout.ts` (с краном торцы — основные, фахверк торцов обнуляется — двойного счёта нет), модель подтверждена пользователем 2026-06-12 (`2026-06-12-end-frame-columns-confirmed.md`). Открытых pending-решений нет.
