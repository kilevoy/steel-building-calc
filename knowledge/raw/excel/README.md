# Excel-источники

Сюда можно локально класть Excel-оракулы для сверки расчётов. Сами файлы `*.xlsx`, `*.xlsm` и похожие форматы не коммитятся.

Каждый файл должен быть описан в `knowledge/raw/source-register.md` с отдельным ID.
